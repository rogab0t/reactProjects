import { styled } from "styled-components";

export const Row = styled.span`
  display: flex;
  flex-direction: row;
  flex-wrap: wrap;
  justify-content: space-around;
  gap: 1em;
  padding: 1em 0;
`;